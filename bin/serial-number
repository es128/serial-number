#!/usr/bin/env node

'use strict';

var path = require('path');
var fs = require('fs');

var serialNumber = require('../');

serialNumber(function (err, val) {
	if (err) throw err;
	console.log(val);
});
